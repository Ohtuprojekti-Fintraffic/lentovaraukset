enum QueryKeys {
  Sample = 'sampleQuery',
  ReservationStatus = 'reservationStatus',
  Reservations = 'reservations',
  TimeSlots = 'timeslots',
  Airfield = 'airfield',
}

export default QueryKeys;
